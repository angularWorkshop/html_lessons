# Solution: Your First HTML Page

This is the complete solution for the "First Page" exercise.

## What was done
- Added HTML5 DOCTYPE declaration
- Created proper `<html>` element with `lang="en"`
- Added `<head>` with charset, viewport, and title
- Added `<body>` with one `<h1>` heading and two `<p>` paragraphs
