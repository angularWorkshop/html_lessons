# Solution: Media Page

This is the complete solution for the "Media Page" exercise.

## What was done
- `<video>` with controls, poster, and fallback text
- `<audio>` with controls and fallback text
- `<iframe>` with title, loading="lazy"
- All wrapped in `<figure>` + `<figcaption>`
