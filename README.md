# Solution: Navigation Menu

This is the complete solution for the "Nav Menu" exercise.

## What was done
- `<nav>` with `aria-label="Main navigation"`
- `<ul>` > `<li>` > `<a>` structure
- `aria-current="page"` on the Home link
