# Solution: Anchor Navigation

This is the complete solution for the "Anchor Nav" exercise.

## What was done
- Table of contents in `<nav>` with anchor links
- `id` attributes on all section headings
- "Back to top" links after each section
- External link with `target="_blank"` and `rel="noopener noreferrer"`
- `mailto:` and `tel:` links
