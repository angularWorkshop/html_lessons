# Solution: Basic CSS Selectors

## Completed
All TODO items have been implemented using tag selectors, class selectors, the universal selector, grouping selectors, and descendant selectors.

## Checklist
- [x] Universal selector `*` resets margin/padding and sets box-sizing
- [x] Tag selectors style `body`, `h1`, `h2`, `h3`, `p`, `a`, `ul`, `li`
- [x] Class selector `.highlight` adds a background color
- [x] Class selector `.card` adds padding, border, and border-radius
- [x] Grouping selector `h1, h2, h3` sets a shared font-family
- [x] Tag selector for `header` and `footer` adds background color
- [x] `nav a` selector removes underline and adds padding
