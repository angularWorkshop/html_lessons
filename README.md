# Solution: Business Card

This is the complete solution for the "Business Card" exercise.

## What was done
- Name in `<h1>`
- Job title in `<p>` with `<strong>`
- Company name in `<strong>`
- `<hr>` separator
- Email as clickable `mailto:` link
- Phone as clickable `tel:` link
- Location in `<p>`
