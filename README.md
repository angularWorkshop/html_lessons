# Solution: Advanced CSS Selectors

## Completed
All advanced selector TODOs have been implemented.

## Checklist
- [x] `h2 + p` adjacent sibling selector styles first paragraph after each h2
- [x] `[href^="http"]` attribute selector styles external links differently
- [x] `[required]` attribute selector highlights required form fields
- [x] `nav > ul` child selector targets only direct ul children of nav
- [x] `[href$=".pdf"]` attribute selector adds a PDF icon indicator
- [x] `:first-child` and `:last-child` pseudo-classes used
- [x] `section:nth-child(even)` alternates section backgrounds
